package avcframewrk.forms

package templating

package rendering



















/**
 * 
 * in case
 * the creation of a new peer would be unacceptably inefficient/expensive,
 * it'd be better to (simply) reuse the existing peer at the place
 * 
 */
@deprecated
final lazy val reconciliabilityC
: templating.rendering.virtualdoms.diffing.engines.c1.type
= templating.rendering.virtualdoms.diffing.engines.c1

@deprecated
final lazy val reconciliabilityCWgc
: templating.rendering.virtualdoms.diffing.engines.c1Wgc.type
= templating.rendering.virtualdoms.diffing.engines.c1Wgc















val _ = {}
